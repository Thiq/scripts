/*

File for an RE Duplex block. These will be blocks that can both input and output RE.

*/
var util = require('util');

var REInput = require('./_re_input');
var REOutput = require('./_re_output');

function REDuplex() {

}

util.inherits(REDuplex, REInput);
util.inherits(REDuplex, REOutput);

module.exports = REDuplex;