function GuiSlot(item, leftCallback, rightCallback, scope) {
    this.item = item;
    this.leftCallback = leftCallback;
    this.rightCallback = rightCallback;
    this.scope = scope;
}

module.exports = GuiSlot;