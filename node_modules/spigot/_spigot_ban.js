var utils = require('./utils');

function banIP(ip) {
    Bukkit.banIP(ip);
}

function banPlayer(player) {

}

function isBanned(player) {
    var offlinePlayer = utils.getOfflinePlayer(player);
    return offlinePlayer && offlinePlayer.isBanned();
}